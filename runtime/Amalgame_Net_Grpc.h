/* amalgame-net-grpc — stub runtime header.
 * The gRPC core (facade.am) is pure Amalgame on List<int> buffers; it
 * carries no C of its own. The HTTP/2 transport wiring (a later
 * milestone) will live in amalgame-net-http's nghttp2 binding. */
#ifndef AMALGAME_NET_GRPC_H
#define AMALGAME_NET_GRPC_H
#endif
